document.addEventListener('DOMContentLoaded',function(){const canvas=document.getElementById('progress-chart');if(!canvas||!window.progressData||!window.progressData.length)return;new Chart(canvas,{type:'line',data:{labels:window.progressData.map(item=>item.date),datasets:[{label:'Screening score',data:window.progressData.map(item=>item.score),borderColor:'#197f7a',backgroundColor:'rgba(25,127,122,.1)',fill:true,tension:.35,pointBackgroundColor:'#197f7a',pointRadius:5}]},options:{responsive:true,maintainAspectRatio:false,scales:{y:{min:0,max:27,ticks:{stepSize:5},grid:{color:'#e5efed'}},x:{grid:{display:false}}},plugins:{legend:{display:false},tooltip:{callbacks:{label:function(context){return ' Score: '+context.raw+'/27';}}}}}});});